import 'package:flutter/material.dart';

class ChooseSign extends StatelessWidget {
  const ChooseSign({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: const Text("Welcome to SIgn"),
      ),
    );
  }
}
